#include <stdio.h>

int main() {
    int l,b,area;
    printf("enter length:");
    scanf("%d",&l);
    printf("enter breadth:");
    scanf("%d",&b);
    area=l*b;
    printf("Area:%d",area);
    return 0;
}
